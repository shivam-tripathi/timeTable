import {WEEKDAYS} from '../../utils/helpers';

const primaryFitness = (chromosome, field, dataObj) => {
    let batches = chromosome

    let keys = WEEKDAYS.slice(0, dataObj.workingDays);

    dataObj.
    keys.map(key => {

    });
}